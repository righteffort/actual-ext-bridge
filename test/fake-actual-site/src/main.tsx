import React from 'react';
import ReactDOM from 'react-dom/client';
import FakeActualApp from './FakeActualApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FakeActualApp />
  </React.StrictMode>
);
